import { DynamoDBClient, PutItemCommand } from "@aws-sdk/client-dynamodb";

const client = new DynamoDBClient({ region: "eu-north-1" });
const TABLE = "Story";
const NOW = Math.floor(Date.now() / 1000);

// ═══════════════════════════════════════════════════════════════════════
//  RUSSIAN (ru)
//  Proverb subverted: "Бог троицу любит"
//  → "Бог троицу любит — и Рим не мелочился: подчинил себе землю,
//     воду и саму смерть, превратив всё это в зрелище для толпы."
// ═══════════════════════════════════════════════════════════════════════
const ru = {
  siteId:             { S: "colosseum-rome" },
  langStoryId:        { S: "ru#naumachia-sea-battles" },
  storyId:            { S: "naumachia-sea-battles" },
  lang:               { S: "ru" },
  storyCategory:      { S: "crowns_conquests" },
  icon:               { S: "🚢" },
  tier:               { S: "A" },
  era:                { S: "Flavian Dynasty (80 AD)" },
  readingTimeMinutes: { N: "3" },
  image:              { S: "" },
  thumbnail:          { S: "" },
  disabled:           { BOOL: false },
  hasAudio:           { BOOL: false },
  isFree:             { BOOL: true },
  coordinates:        { M: { lat: { N: "41.8902" }, lng: { N: "12.4922" } } },
  source:             { S: "Martial, De Spectaculis (Liber Spectaculorum); Cassius Dio, Roman History LXVI; Suetonius, Lives of the Caesars" },
  updatedAt:          { N: String(NOW) },

  title:    { S: "Когда Колизей стал морем" },
  subtitle: { S: "Как Рим затопил арену ради настоящего морского боя" },

  excerpt: { S: "Представьте себе: на дворе 80 год нашей эры, Колизей в Риме только-только распахнул ворота, и император Тит решает — бои гладиаторов для такого события мелковаты. Что он делает? Затапливает арену целиком и устраивает настоящее морское сражение. Боевые корабли в полный размер. Заточенное оружие. Люди гибнут по-настоящему." },

  moralOrLesson: { S: "Великие империи стремятся доказать власть не только над людьми, но и над самой природой — однако даже самые грандиозные зрелища рано или поздно уступают ходу времени." },

  characters: { L: [
    { S: "Император Тит" },
    { S: "Император Домициан" },
    { S: "Марциал (поэт)" },
    { S: "Кассий Дион (историк)" },
    { S: "Приговорённые к смерти" },
  ]},

  paragraphs: { L: [
    { M: { text: { S: "Представьте себе: на дворе 80 год нашей эры, Колизей в Риме только-только распахнул ворота, и император Тит решает — бои гладиаторов для такого события мелковаты. Что он делает? Затапливает арену целиком и устраивает настоящее морское сражение. Боевые корабли в полный размер. Заточенное оружие. Люди гибнут по-настоящему. Звучит как безумие — но несколько очевидцев записали увиденное, а современные археологи нашли ту самую инженерию, которая сделала всё это возможным." }}},

    { M: { text: { S: "В толпе в тот день оказался поэт по имени Марциал. Он видел всё своими глазами и описал увиденное в сборнике стихов об играх в честь открытия. Тем, кто пришёл позже и увидел воду вместо земли, он говорил примерно так: не верьте глазам — утром тут было сухо, а к ночи снова будет. Там, где пару часов назад гладиаторы бились на твёрдом грунте, сталкивались боевые корабли. Арена превращалась из суши в море и обратно — словно само здание не могло решить, чем ему быть." }}},

    { M: { text: { S: "Историк Кассий Дион добавил подробности, от которых кровь стынет в жилах. Тит не просто спустил на воду пару лодок — он воссоздал знаменитые морские битвы из греческой истории: Афины против Сиракуз. Рядом с кораблями в воде действовали лошади и быки, специально обученные для таких зрелищ. А бойцами были смертники — им вручили настоящие мечи и заставили изображать античных моряков. Никакой постановки, никакой страховки. Клинки рубили всерьёз, люди тонули, а вода становилась красной." }}},

    { M: { text: { S: "Инженерная сторона до сих пор поражает специалистов. Под полом арены римляне проложили сеть каналов, подключённых к городским акведукам. Гигантские заслонки регулировали поток воды, а пол был залит водонепроницаемым бетоном, чтобы ни капли не просочилось в помещения ниже. После представления дренажная система осушала арену за считанные часы. По сути, они построили стадион, который работал ещё и как бассейн. Только в этом бассейне не плавали — в нём убивали." }}},

    { M: { text: { S: "Морские бои продержались от силы десять лет. Домициан — брат и преемник Тита — решил, что пространство под ареной выгоднее превратить в постоянное закулисье. Он выстроил подземный лабиринт из тоннелей, звериных клеток и механических подъёмников — те самые руины, которые сегодня видит каждый турист в Колизее. Как только появились деревянные перекрытия и вся эта механика, затопить арену стало невозможно. Самое грандиозное шоу в истории Рима исчезло — раз и навсегда." }}},

    { M: { text: { S: "Но главное здесь — не как, а зачем. Римляне называли Средиземное море «Mare Nostrum» — «Наше море». Затащив океан в своё главное здание, императоры заявляли: мы командуем не только сушей — мы командуем и водой. Бог троицу любит — и Рим не мелочился: подчинил себе землю, воду и саму смерть, превратив всё это в зрелище для толпы. Для пятидесяти тысяч человек, видевших, как корабли идут на таран посреди их города, вывод был один: Риму подвластно всё." }}},
  ]},
};

// ─── Validation ─────────────────────────────────────────────────────
console.log("═══════════════════════════════════════════════");
console.log("  Naumachia — RUSSIAN (ru) — Validation");
console.log("═══════════════════════════════════════════════\n");

const pCount = ru.paragraphs.L.length;
console.log(`Paragraphs: ${pCount} (target 6-8)`);
if (pCount < 6 || pCount > 10) {
  console.error(`❌ Paragraph count ${pCount} outside 6-10 range`);
  process.exit(1);
}

let totalChars = 0;
let allValid = true;

ru.paragraphs.L.forEach((p, i) => {
  const text = p.M.text.S;
  const chars = text.length;
  const words = text.split(/\s+/).length;
  totalChars += chars;

  const charOk = chars <= 500;
  const wordOk = words <= 100;
  if (!charOk || !wordOk) allValid = false;

  console.log(`  P${i + 1}: ${chars} chars ${charOk ? "✅" : "❌"} | ${words} words ${wordOk ? "✅" : "❌"}`);
});

console.log(`\nTotal characters: ${totalChars} (target ~3000, ±20% = 2400-3600)`);
const totalOk = totalChars >= 2400 && totalChars <= 3600;
console.log(`Total in range: ${totalOk ? "✅" : "❌"}`);

console.log(`\nExcerpt: ${ru.excerpt.S.length} chars`);
console.log(`Title: ${ru.title.S}`);
console.log(`Subtitle: ${ru.subtitle.S}`);

if (!allValid || !totalOk) {
  console.error("\n❌ Validation failed. Aborting.");
  process.exit(1);
}

// ─── Push ───────────────────────────────────────────────────────────
console.log("\n▸ Pushing RUSSIAN (ru) to DynamoDB...");

try {
  const cmd = new PutItemCommand({ TableName: TABLE, Item: ru });
  const result = await client.send(cmd);
  console.log(`✅ Russian story pushed successfully (HTTP ${result.$metadata.httpStatusCode})`);
  console.log(`   langStoryId: ru#naumachia-sea-battles`);
  console.log(`   updatedAt: ${NOW}`);
} catch (err) {
  console.error("❌ Push failed:", err.message);
  process.exit(1);
}
