import { DynamoDBClient } from "@aws-sdk/client-dynamodb";
import { DynamoDBDocumentClient, PutCommand } from "@aws-sdk/lib-dynamodb";

const client = new DynamoDBClient({ region: "eu-north-1" });
const docClient = DynamoDBDocumentClient.from(client, {
  marshallOptions: { removeUndefinedValues: true },
});

const TABLE = "Story";
const now = Math.floor(Date.now() / 1000);

// ──────────────────────────────────────────────
// RUSSIAN VERSION
// ──────────────────────────────────────────────

const ruParagraphs = [
  `В середине XV века во всей Венгрии не было человека могущественнее Яноша Хуньяди — и не было человека, которого так хотели уничтожить. Величайший полководец королевства, он в одиночку остановил Османскую империю на подступах к Центральной Европе. Народ его обожал, знать — ненавидела. Заговоры и покушения были для Хуньяди не угрозой, а повседневностью. В 1443 году он снова уходил в поход — и перед отъездом принял решение, которое случайно породит самую невероятную легенду Венгрии.`,

  `Перед выступлением Янош передал перстень-печатку беременной жене, Эржебет Силадьи. Это было не украшение — это была власть. Тяжёлая золотая печать, которой скрепляли договоры и отдавали приказы армиям. Кто владел этим перстнем — говорил от имени самого Хуньяди. А в мире, где одно подложное письмо могло развязать войну или стоить кому-то головы, потеря печати означала катастрофу.`,

  `Несколько недель спустя Эржебет родила сына — Матьяша. Однажды утром в замке Хуньяд она положила перстень на край колыбели и на мгновение отвлеклась. Этого хватило. Чёрный ворон влетел в распахнутое окно, цепко ухватил клювом золотое кольцо — и в два взмаха крыльев оказался на верхушке башни. Самый ценный предмет в доме Хуньяди исчез за считаные секунды.`,

  `Эржебет похолодела. Без перстня враги мужа могли подделать его печать, разослать ложные приказы, поднять против него его же войска. Но то, что случилось дальше, не поддавалось объяснению. Младенец Матьяш — которому от роду было несколько недель — вдруг уставился на ворона. Не заплакал, не отвернулся. Просто смотрел — неподвижным, немигающим взглядом, от которого даже взрослому стало бы не по себе. Ворон замер. Прошла минута, другая. А потом птица медленно спустилась, села на край колыбели и уронила перстень прямо на одеяло малыша.`,

  `Слух разлетелся по Венгрии быстрее любого гонца. «Вороны признали его. Этот ребёнок отмечен судьбой». Говорят, Бог троицу любит — но Матьяшу хватило одного ворона и одного взгляда, чтобы целая страна поверила: он рождён для престола. Был ли ворон ручным? Придумали ли историю позже, чтобы обернуть будущего короля в ореол избранности? В пятнадцатом веке это было неважно. Вера в ту эпоху двигала народы — а верили все.`,

  `И верили не зря. В 1458 году пятнадцатилетнего Матьяша избрали королём Венгрии — в обход более знатных и богатых претендентов, потому что народ потребовал именно его. Он взял ворона своим символом и вошёл в историю как Матьяш Корвин — буквально «Матьяш-Ворон». На его гербе красовался чёрный ворон с золотым перстнем в клюве — та самая птица, то самое кольцо, та самая легенда, ставшая государственным знаком.`,

  `Этот ворон и сегодня смотрит на нас — высеченный в камне, отчеканенный на монументах, вплетённый в ткань венгерской истории. Младенец, который взглядом остановил дикую птицу, вырос и остановил Османскую империю. Построил один из великих ренессансных дворов Европы. Стал величайшим королём, которого Венгрия когда-либо знала. Было ли всё это на самом деле — уже неважно. Есть легенды, которые не просто описывают величие. Они его создают.`,
];

const ruItem = {
  siteId: "buda-castle",
  langStoryId: "ru#raven-king",
  storyId: "raven-king",
  lang: "ru",
  title: "Ворон и корона",
  subtitle: "Как Матьяш Корвин получил своё имя",
  excerpt:
    "В середине XV века во всей Венгрии не было человека могущественнее Яноша Хуньяди — и не было человека, которого так хотели уничтожить. Величайший полководец королевства, он в одиночку остановил Османскую империю на подступах к Центральной Европе.",
  paragraphs: ruParagraphs.map((text) => ({ text })),
  moralOrLesson:
    "Судьба не всегда приходит с громом — иногда хватает взмаха крыла.",
  characters: [
    "Matthias Corvinus (infant)",
    "Erzsébet Szilágyi",
    "János Hunyadi",
    "The Raven",
  ],
  coordinates: { lat: 47.4961, lng: 19.0398 },
  era: "1443",
  icon: "\u{1F985}",
  image: "",
  thumbnail: "",
  source:
    "János Thuróczy's Chronica Hungarorum; Hungarian folk tradition",
  storyCategory: "crowns_conquests",
  tier: "A",
  isFree: true,
  hasAudio: false,
  disabled: false,
  readingTimeMinutes: 2,
  updatedAt: now,
};

// ──────────────────────────────────────────────
// PUSH TO DYNAMODB
// ──────────────────────────────────────────────

async function main() {
  console.log(`\n${"=".repeat(50)}`);
  console.log("Pushing Russian (ru) raven-king...");
  console.log(`  siteId:      ${ruItem.siteId}`);
  console.log(`  langStoryId: ${ruItem.langStoryId}`);
  console.log(`  title:       ${ruItem.title}`);
  console.log(`  paragraphs:  ${ruItem.paragraphs.length}`);

  // Validate JSON serialization
  const json = JSON.stringify(ruItem);
  JSON.parse(json); // will throw if invalid
  console.log(`  JSON valid:  \u2713 (${json.length} bytes)`);

  const cmd = new PutCommand({ TableName: TABLE, Item: ruItem });
  const result = await docClient.send(cmd);
  console.log(`  HTTP status: ${result.$metadata.httpStatusCode}`);

  if (result.$metadata.httpStatusCode === 200) {
    console.log(`  \u2705 Russian (ru) pushed successfully!`);
    console.log(`  updatedAt:   ${now}`);
  } else {
    throw new Error(
      `Unexpected status: ${result.$metadata.httpStatusCode}`
    );
  }

  console.log(`${"=".repeat(50)}\n`);
}

main().catch((err) => {
  console.error("\u274C Error:", err);
  process.exit(1);
});
